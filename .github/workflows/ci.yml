name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2
    - name: Check docker
      run: which docker
    # - name: Install homebrew
      # run: python3 -m pip install -r requirements.txt
    # - name: Run tests
      # run: python3 -m unittest discover -s mapdump/tests
